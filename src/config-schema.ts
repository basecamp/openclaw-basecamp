/**
 * Re-export the Basecamp config schema from config.ts.
 *
 * The authoritative schema is defined in config.ts alongside the
 * config adapter helpers. This file exists for import convenience.
 */
export { BasecampConfigSchema } from "./config.js";
